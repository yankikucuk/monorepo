# @april/interface

## 0.2.0

### Minor Changes

- 4318551: Foundation of the framework: colour tokens with derived states for seven shared roles, light and dark themes with a persisted toggle, base element styles, typography utilities, links (`ai-link` with plain, subtle, arrow, external, nav and breadcrumb patterns), buttons (`ai-button` with solid, outline, ghost, tonal and link styles, global shape and size modifiers, loading and disabled states, button groups), layout primitives (container, stack, cluster) and the application shell used by the demo pages.
- b8bf26f: Add the opt-in icon stylesheet: Font Awesome Pro solid icons compiled to `dist/icons.css` (and `.min.css`) with the webfont copied next to it, exported as `@april/interface/icons.css`.
