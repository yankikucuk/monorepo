# Font Awesome Pro lives on its own registry. The auth token is not stored here:
# put `//npm.fontawesome.com/:_authToken=<token>` in your user-level ~/.npmrc
# (CI writes it from the FONTAWESOME_TOKEN secret).
@fortawesome:registry=https://npm.fontawesome.com/
