---
'@april/eslint-config': patch
---

Upgrade the linting toolchain: `eslint-plugin-unicorn` 72 → 74, `eslint-plugin-jsdoc` 63 → 64, `typescript-eslint` 8.64 → 8.69, plus patch releases of `eslint-plugin-check-file`, `eslint-plugin-perfectionist`, `eslint-plugin-regexp` and `globals`. No rule configuration changed; every existing source still lints clean.
